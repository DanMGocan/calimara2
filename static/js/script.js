// All client-side JavaScript logic for form submissions and UI updates has been removed.
// The application now relies entirely on server-side rendering with FastAPI and Jinja2.
// Forms submit directly to FastAPI endpoints, and FastAPI handles redirects and renders new pages.
